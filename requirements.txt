# ── 後端 API（game_api/） ───────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.46.0
SQLAlchemy==2.0.49
PyMySQL==1.1.3
python-jose==3.5.0
python-multipart==0.0.27   # 解析 Form 表單欄位（auth.py 的 /google 用 Form 接 id_token）
pydantic==2.13.3
python-dotenv==1.2.2
google-auth==2.53.0   # 驗證 Google 登入回傳的 id_token（OpenID Connect）
requests==2.33.1      # google-auth 抓取 Google 公開憑證所需的 HTTP 傳輸層

# ── 數學引擎與模擬器（core/, simulator/） ────────────────────────────────────────
numpy==2.4.4

# ── 分析儀表板（dashboard/） ────────────────────────────────────────────────────
streamlit==1.56.0
plotly==6.7.0
pandas==3.0.2

# ── 測試（tests/） ──────────────────────────────────────────────────────────────
pytest==9.0.3
httpx==0.28.1
